// Local-workstation override: this machine has ~15 GB RAM, below the
// process_medium (16 GB) / process_high (32 GB) tiers in conf/base.config.
// Same approach as conf/integration.config uses for GitHub Actions runners.
process {
    resourceLimits = [ cpus: 4, memory: '13.GB', time: '12.h' ]
}
